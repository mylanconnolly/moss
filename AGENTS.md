# moss — agent orientation

Every agent working in this repository follows `CLAUDE.md`: the commands,
the gate, what to read before changing things, and the habits that fit
this repo. It is the one orientation file; this pointer exists so tools
that look for `AGENTS.md` find it and do not drift from it.
