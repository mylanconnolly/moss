# The network service's settings, cluster addressing: the resolver is the
# node's own dnsd on loopback (dnsd-cluster serves the fabric's node-name
# zone), so `node1.moss.test` resolves without a slirp gateway.
{ resolvers: [::1] }
