# The network service (virtio-net + dual-stack TCP), cluster addressing
# as node 1 (arg = 1 | node << 8).
{
  image: net
  arg:   257
  give: [
    { tag: mmio, device: mmio }
    { tag: irq,  device: irq }
  ]
}
