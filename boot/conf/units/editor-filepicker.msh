# The editor drill's broker, confined to its disposable disk view; it
# starts the `chooser` unit through init for dialogs.
{
  image:  filepicker
  arg:    1
  budget: { user: 16mb }
  grant:  [log]
  give: [
    { tag: init, self: true }
    { tag: view, fs: data, ro: false }
  ]
}
