# The DNS-over-TLS drill: a script resolves a name; netsvc's resolver
# forwards the query through dotd, which asks the upstream over TLS. The
# name is one only the DoT responder answers, so a right answer proves
# the private path end to end.
{ image: mshrun, script: scripts/dot-drill.msh, grant: [log, bootfs], profiles: [dot], oneshot: true, essential: true,
  give: [{ tag: net, netview: net }] }
